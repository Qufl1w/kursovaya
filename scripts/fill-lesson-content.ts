import dotenv from 'dotenv'
dotenv.config({ path: '.env.local' })
import { db } from '../lib/db'
import { lessons } from '../lib/db/schema'
import { eq } from 'drizzle-orm'

const content: Record<string, string> = {
  'Введение в JavaScript': `JavaScript — это язык программирования, который изначально создавался для добавления интерактивности на веб-страницы, а сегодня используется и на сервере (через Node.js), и в мобильной разработке, и даже в десктопных приложениях.

Код на JavaScript выполняется в браузере (движком вроде V8 в Chrome) или в Node.js вне браузера. В отличие от компилируемых языков (C++, Java), JavaScript — интерпретируемый и динамически типизированный язык: тип переменной не указывается явно и может меняться во время выполнения программы.

Простейшая программа на JavaScript:
console.log("Привет, мир!");

Эта команда выведет текст в консоль браузера или терминал, если запускать через Node.js.`,

  'Переменные и типы данных': `Переменная — это именованная область памяти для хранения значения. В JavaScript переменные объявляются тремя способами: var (устаревший), let (можно менять значение) и const (нельзя менять после создания).

let age = 18;
const name = "Костя";
age = 19; // сработает
name = "Иван"; // ошибка, const нельзя переприсвоить

Основные типы данных: number (числа), string (строки), boolean (true/false), undefined, null, object (объекты) и array (массивы, по сути тоже объекты). JavaScript — язык с динамической типизацией: одна и та же переменная может сначала хранить число, а потом строку, без явного объявления типа.`,

  'Условия и циклы': `Условия позволяют выполнять разный код в зависимости от ситуации. Основная конструкция — if/else:

if (age >= 18) {
  console.log("Совершеннолетний");
} else {
  console.log("Несовершеннолетний");
}

Циклы нужны для повторения действий. Самые частые — for (когда известно количество повторений) и while (когда повторяем, пока выполняется условие):

for (let i = 0; i < 5; i++) {
  console.log(i);
}

let i = 0;
while (i < 5) {
  console.log(i);
  i++;
}

Также есть цикл for...of для перебора элементов массива и for...in для перебора ключей объекта.`,

  'Что такое Node.js': `Node.js — это среда выполнения (runtime), которая позволяет запускать JavaScript вне браузера, например на сервере. Он построен на движке V8 (том же, что используется в Chrome).

До появления Node.js JavaScript мог работать только в браузере. Node.js дал возможность писать серверную часть приложений (backend) на том же языке, что и клиентскую (frontend) — это удобно, так как не нужно переключаться между двумя разными языками.

Node.js использует неблокирующую, событийно-ориентированную модель ввода-вывода — то есть он может обрабатывать много запросов одновременно, не создавая отдельный поток на каждый запрос, что делает его эффективным для сетевых приложений.`,

  'Первый Express сервер': `Express — это минималистичный фреймворк для Node.js, который упрощает создание серверов и REST API. Без него пришлось бы вручную обрабатывать HTTP-запросы через встроенный модуль http, что гораздо более многословно.

Простейший сервер на Express выглядит так:

const express = require('express');
const app = express();

app.get('/', (req, res) => {
  res.send('Привет от сервера!');
});

app.listen(3000, () => {
  console.log('Сервер запущен на порту 3000');
});

Здесь app.get описывает маршрут (route) — что должно происходить, если пользователь заходит на главную страницу через GET-запрос. Middleware — это функции, которые выполняются между получением запроса и отправкой ответа, например для проверки авторизации или логирования.`,

  'Введение в PostgreSQL': `PostgreSQL — это объектно-реляционная система управления базами данных (СУБД) с открытым исходным кодом. Она хранит данные в виде таблиц со строками и столбцами, а связи между таблицами задаются через внешние ключи (foreign keys).

Базовые операции с данными выполняются на языке SQL (Structured Query Language):

SELECT * FROM users WHERE id = 1;
INSERT INTO users (name, email) VALUES ('Костя', 'test@mail.com');
UPDATE users SET name = 'Иван' WHERE id = 1;
DELETE FROM users WHERE id = 1;

PostgreSQL поддерживает транзакции (группу операций, которые выполняются все вместе или ни одна), индексы для быстрого поиска и расширения — например, pgvector, которое добавляет поддержку векторного поиска прямо в базу данных.`,
}

async function main() {
  const { db } = await import('../lib/db')

  for (const [title, text] of Object.entries(content)) {
    const res = await db.update(lessons).set({ content: text }).where(eq(lessons.title, title))
    console.log(`Обновлён урок: ${title}`)
  }
  console.log('Готово!')
  process.exit(0)
}

main()